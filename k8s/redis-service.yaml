apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: chaos-sandbox
  labels:
    app: chaos-redis
spec:
  type: ClusterIP
  selector:
    app: chaos-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379